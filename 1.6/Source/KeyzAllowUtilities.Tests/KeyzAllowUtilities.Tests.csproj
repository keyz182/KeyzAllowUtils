<?xml version="1.0" encoding="utf-8"?>

<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <TargetFrameworks>net48;net8.0</TargetFrameworks>
        <LangVersion>latest</LangVersion>
        <RootNamespace>KeyzAllowUtilities.Tests</RootNamespace>
        <AssemblyName>KeyzAllowUtilities.Tests</AssemblyName>
        <OutputType>Exe</OutputType>
        <GenerateAssemblyInfo>true</GenerateAssemblyInfo>
        <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    </PropertyGroup>
    <!-- Game DLL references — path resolved by Directory.Build.props/targets -->
    <ItemGroup Condition="'$(TargetFramework)' == 'net48' and '$(RimWorldManagedDir)' != ''">
        <Reference Include="Assembly-CSharp">
            <HintPath>$(RimWorldManagedDir)/Assembly-CSharp.dll</HintPath>
            <Private>False</Private>
        </Reference>
        <Reference Include="UnityEngine.CoreModule">
            <HintPath>$(RimWorldManagedDir)/UnityEngine.CoreModule.dll</HintPath>
            <Private>False</Private>
        </Reference>
    </ItemGroup>
    <!-- net8.0: copy game DLLs to test output so the .NET 8 runtime can load them -->
    <ItemGroup Condition="'$(TargetFramework)' == 'net8.0' and '$(RimWorldManagedDir)' != ''">
        <Reference Include="Assembly-CSharp">
            <HintPath>$(RimWorldManagedDir)/Assembly-CSharp.dll</HintPath>
            <Private>True</Private>
        </Reference>
        <Reference Include="UnityEngine.CoreModule">
            <HintPath>$(RimWorldManagedDir)/UnityEngine.CoreModule.dll</HintPath>
            <Private>True</Private>
        </Reference>
    </ItemGroup>
    <ItemGroup>
        <PackageReference Include="JetBrains.Annotations" Version="2025.2.2" />
        <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.13.0" />
        <PackageReference Include="xunit.runner.visualstudio" Version="3.1.1" />
        <PackageReference Include="xunit.v3" Version="3.2.0" />
    </ItemGroup>
    <ItemGroup>
        <ProjectReference Include="../KeyzAllowUtilities/KeyzAllowUtilities.csproj" />
    </ItemGroup>
    <Target Name="RunTestsAfterBuild" AfterTargets="Build" Condition="'$(TargetFramework)' == 'net8.0'">
        <Exec Command="dotnet test --no-build --framework net8.0 --logger console;verbosity=minimal"
              WorkingDirectory="$(MSBuildProjectDirectory)" />
    </Target>
</Project>
